cat SmartAttendanceBackend/requirements.txt
```

If you still see `deepface` or `tf-keras` in it, the file was not replaced.

**Step 2 — Replace it completely. Copy-paste this exact content:**
```
fastapi
uvicorn
sqlalchemy
psycopg2-binary
python-multipart
python-dotenv
pillow
numpy
opencv-python-headless
face-recognition
